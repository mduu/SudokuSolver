<?xml version="1.0" encoding="utf-8"?>

<Grid xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:SudokuSolver.Mobile.Controls;assembly=SudokuSolver.Mobile"
             x:Class="SudokuSolver.Mobile.Controls.SectionControl"
             x:DataType="controls:SectionControl"
             x:Name="this">
    
    <Grid.RowDefinitions>
        <RowDefinition></RowDefinition>
        <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    
    <Label Text="{Binding SectionName}" />
    <Label Text="{Binding Test}" Grid.Row="1" />
</Grid>